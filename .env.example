# Kopieren nach .env und ausfuellen. .env ist gitignored.

# Port, unter dem Nuvora erreichbar ist
PORT=8080
SITE_URL=http://localhost:8080

# Postgres
POSTGRES_USER=nuvora
POSTGRES_PASSWORD=   # PFLICHT: eigenes starkes Passwort setzen
POSTGRES_DB=nuvora

# PFLICHT: eigener Zufallswert, z.B. `openssl rand -hex 32`
TOKEN_SECRET=

# Admin-Konto (beide Module erkennen den Admin an dieser Adresse)
ADMIN_EMAIL=
ADMIN_PASSWORD=

# E-Mail-Versand (Registrierung, Passwort-Reset)
SMTP_HOST=
SMTP_PORT=465
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_FROM_NAME=Nuvora
