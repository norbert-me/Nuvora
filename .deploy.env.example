# Kopieren nach .deploy.env und eintragen. .deploy.env ist gitignored.
SERVER="root@dein-server"
REMOTE_DIR="/pfad/auf/dem/server/nuvora"

# Muss zu PORT/SITE_URL in der .env des Servers passen (nur fuer Health-Check
# und die Ausgabe am Ende).
PORT=8080
SITE_URL="https://nuvora.deine-domain.de"
